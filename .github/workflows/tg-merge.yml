name: Merge Alert

on:
  pull_request:
    types: [closed]

jobs:
  notify:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - run: |
          TIMESTAMP=$(TZ='Asia/Phnom_Penh' date +"%b %d at %I:%M %p")
          MERGE_SHA=$(echo '${{ github.event.pull_request.merge_commit_sha }}' | cut -c1-7)
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage" \
          -d chat_id=${{ secrets.TG_CHAT_ID }} \
          -d message_thread_id=$TG_TOPIC_ID \
          -d parse_mode=Markdown \
          -d text=$'✅ *MERGED* 🎉\n╙═════════════════════╜\n\n🏷️ *PR #${{ github.event.pull_request.number }}*\n📌 _${{ github.event.pull_request.title }}_\n\n👑 *Merged by:* @${{ github.actor }}\n🌿 *Into:* `${{ github.base_ref }}`\n⏱️ *Shipped:* '"$TIMESTAMP"$' ICT\n\n💥 *Impact:*\n• ${{ github.event.pull_request.changed_files }} files changed\n• ${{ github.event.pull_request.commits }} commits squashed\n• +${{ github.event.pull_request.additions }} / -${{ github.event.pull_request.deletions }} lines\n• Merge hash: `'"$MERGE_SHA"$'`\n\n🔗 [See the PR](${{ github.event.pull_request.html_url }})\n╚═════════════════════╝\n🚀 Another W in the books. Deploy szn 🔥'

env:
  TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
  TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
  TG_TOPIC_ID: ${{ secrets.TG_TOPIC_ID }}
